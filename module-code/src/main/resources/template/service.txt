package com.nowui.cloud.${moduleName}.${packageName}.service;

import com.nowui.cloud.service.BaseService;
import com.nowui.cloud.${moduleName}.${packageName}.entity.${firstUpperWithoutUnderlineEntityName};
import com.nowui.cloud.${moduleName}.${packageName}.view.${firstUpperWithoutUnderlineEntityName}View;
import java.util.List;
<%if(isDate){%>
import java.util.Date;
<%}%>

/**
 * ${tableComment}业务接口
 *
 * @author ${author}
 *
 * ${date}
 */
public interface ${firstUpperWithoutUnderlineEntityName}Service extends BaseService<${firstUpperWithoutUnderlineEntityName}, ${firstUpperWithoutUnderlineEntityName}View> {

    /**
     * ${tableComment}统计
     *
     * @param appId 应用编号
     <%for(column in searchColumnList){%>
     * @param ${column.columnName} ${column.columnComment}
     <%}%>
     * @return Integer ${tableComment}统计
     */
    Integer countForAdmin(String appId<%for(column in searchColumnList){%>, <%if(column.dataType == "TINYINT"){%>Boolean<%}else if(column.dataType == "INT"){%>Integer<%}else if(column.dataType == "DECIMAL"){%>BigDecimal<%}else if(column.dataType == "DATETIME"){%>Date<%}else{%>String<%}%> ${column.columnName}<%}%>);

    /**
     * ${tableComment}列表
     *
     * @param appId 应用编号
     <%for(column in searchColumnList){%>
     * @param ${column.columnName} ${column.columnComment}
     <%}%>
     * @param pageIndex 页码
     * @param pageSize 每页个数
     * @return List<${firstUpperWithoutUnderlineEntityName}> ${tableComment}列表
     */
    List<${firstUpperWithoutUnderlineEntityName}View> listForAdmin(String appId<%for(column in searchColumnList){%>, <%if(column.dataType == "TINYINT"){%>Boolean<%}else if(column.dataType == "INT"){%>Integer<%}else if(column.dataType == "DECIMAL"){%>BigDecimal<%}else if(column.dataType == "DATETIME"){%>Date<%}else{%>String<%}%> ${column.columnName}<%}%>, Integer pageIndex, Integer pageSize);

}