package com.nowui.cloud.${moduleName}.${packageName}.service.impl;

import com.nowui.cloud.mybatisplus.BaseWrapper;
import com.nowui.cloud.service.impl.BaseServiceImpl;
import com.nowui.cloud.${moduleName}.${packageName}.entity.${firstUpperEntityName};
import com.nowui.cloud.${moduleName}.${packageName}.mapper.${firstUpperEntityName}Mapper;
import com.nowui.cloud.${moduleName}.${packageName}.service.${firstUpperEntityName}Service;
import org.springframework.stereotype.Service;

import java.util.Arrays;
import java.util.List;

/**
 * @author ${author}
 */
@Service
public class ${firstUpperEntityName}ServiceImpl extends BaseServiceImpl<${firstUpperEntityName}Mapper, ${firstUpperEntityName}> implements ${firstUpperEntityName}Service {

    @Override
    public Integer adminCount(String appId<%for(column in searchColumnList){%>, String ${column.columnName}<%}%>) {
        Integer count = count(
                new BaseWrapper<Product>()
                        .eq(Product.APP_ID, appId)
                        <%for(column in searchColumnList){%>
                        .likeAllowEmpty(${firstUpperEntityName}.${column.upperColumnName}, ${column.columnName})
                         <%}%>
                        .eq(${firstUpperEntityName}.SYSTEM_STATUS, true)
        );
        return count;
    }

    @Override
    public List<${firstUpperEntityName}> adminList(String appId<%for(column in searchColumnList){%>, String ${column.columnName}<%}%>, Integer m, Integer n) {
        List<${firstUpperEntityName}> ${firstLowerEntityName}List = list(
                new BaseWrapper<${firstUpperEntityName}>()
                        .eq(Product.APP_ID, appId)
                        <%for(column in searchColumnList){%>
                        .likeAllowEmpty(${firstUpperEntityName}.${column.upperColumnName}, ${column.columnName})
                         <%}%>
                        .eq(${firstUpperEntityName}.SYSTEM_STATUS, true)
                        .orderDesc(Arrays.asList(${firstUpperEntityName}.SYSTEM_CREATE_TIME)),
                m,
                n
        );

        return ${firstLowerEntityName}List;
    }

}