package com.nowui.cloud.${moduleName}.${packageName}.service;
import com.nowui.cloud.service.BaseService;
import com.nowui.cloud.${moduleName}.${packageName}.entity.${firstUpperEntityName};

import java.util.List;

/**
 * ${tableComment}业务接口
 *
 * @author ${author}
 *
 * ${date}
 */
public interface ${firstUpperEntityName}Service extends BaseService<${firstUpperEntityName}> {

    /**
     * 统计
     *
     * @param appId 应用编号
     <%for(column in searchColumnList){%>
     * @param ${column.columnName} ${column.columnComment}
     <%}%>
     * @return Integer 统计
     */
    Integer adminCount(String appId<%for(column in searchColumnList){%>, String ${column.columnName}<%}%>);

    /**
     * 列表
     *
     * @param appId 应用编号
     <%for(column in searchColumnList){%>
     * @param ${column.columnName} ${column.columnComment}
     <%}%>
     * @param m 从m条开始
     * @param n 取n条数据
     * @return List<${firstUpperEntityName}> 列表
     */
    List<${firstUpperEntityName}> adminList(String appId<%for(column in searchColumnList){%>, String ${column.columnName}<%}%>, Integer m, Integer n);
}
